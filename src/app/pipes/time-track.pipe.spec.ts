import { TimeTrackPipe } from './time-track.pipe';

describe('TimeTrackPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeTrackPipe();
    expect(pipe).toBeTruthy();
  });
});
